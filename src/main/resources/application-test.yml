# Profil TEST : la datasource est fournie par Testcontainers (@ServiceConnection).
spring:
  jpa:
    show-sql: false
  flyway:
    locations: classpath:db/migration    # pas de jeu de démo dans les tests

logging:
  level:
    com.jodak: INFO
