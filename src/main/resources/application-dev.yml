# Profil DEV : PostgreSQL local + jeu de démonstration (db/seed).
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/olympics
    username: olympics
    password: olympics
  jpa:
    show-sql: true
  flyway:
    locations: classpath:db/migration,classpath:db/seed

logging:
  level:
    com.jodak: DEBUG
    org.hibernate.SQL: DEBUG
